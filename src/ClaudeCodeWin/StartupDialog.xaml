<Window x:Class="ClaudeCodeWin.StartupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ClaudeCodeWin"
        Width="560" Height="480"
        MinWidth="400" MinHeight="350"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Welcome back!"
                   FontSize="22" FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}"
                   Margin="0,0,0,16"/>

        <!-- New session option -->
        <Border Grid.Row="1" x:Name="NewSessionOption"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="6" Padding="12,10" Margin="0,0,0,16"
                Cursor="Hand"
                MouseLeftButtonUp="NewSession_Click"
                BorderThickness="2"
                BorderBrush="{StaticResource PrimaryBrush}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="+" FontSize="18" FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrush}"
                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                <StackPanel>
                    <TextBlock Text="Start new session"
                               FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"/>
                    <TextBlock Text="Begin a fresh conversation"
                               FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Recent sessions list -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Recent conversations"
                       FontSize="13" FontWeight="SemiBold"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,0,0,8"/>

            <ListView Grid.Row="1" x:Name="SessionList"
                      Background="Transparent"
                      BorderThickness="0"
                      Foreground="{StaticResource TextBrush}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      MouseDoubleClick="SessionList_MouseDoubleClick"
                      SelectionChanged="SessionList_SelectionChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,4"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="Bd" Background="{StaticResource SurfaceBrush}"
                                            Padding="12,8"
                                            CornerRadius="6"
                                            BorderThickness="2"
                                            BorderBrush="Transparent">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="BorderBrush"
                                                    Value="{StaticResource PrimaryBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{StaticResource SurfaceLightBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="{Binding Title}"
                                       FontSize="13" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       TextTrimming="CharacterEllipsis"/>

                            <TextBlock Grid.Row="0" Grid.Column="1"
                                       Text="{Binding UpdatedAt, StringFormat='{}{0:MMM dd, HH:mm}'}"
                                       FontSize="11"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"/>

                            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                        Orientation="Horizontal" Margin="0,3,0,0">
                                <TextBlock Text="{Binding ProjectName}"
                                           FontSize="11"
                                           Foreground="{StaticResource PrimaryBrush}"/>
                                <TextBlock FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="10,0,0,0">
                                    <Run Text="{Binding MessageCount, Mode=OneWay}"/>
                                    <Run Text="messages"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Continue button -->
        <Button Grid.Row="3" x:Name="ContinueButton"
                Content="Continue"
                Style="{StaticResource PrimaryButton}"
                Click="ContinueButton_Click"
                HorizontalAlignment="Center"
                Padding="40,8"
                Margin="0,16,0,0"/>

        <!-- Hint -->
        <TextBlock Grid.Row="4"
                   Text="Press Enter to start new session, or select a conversation above"
                   FontSize="11" FontStyle="Italic"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,10,0,0"/>
    </Grid>
</Window>

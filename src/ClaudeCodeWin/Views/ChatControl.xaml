<UserControl x:Class="ClaudeCodeWin.Views.ChatControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ClaudeCodeWin.ViewModels"
             xmlns:converters="clr-namespace:ClaudeCodeWin.Converters"
             x:Name="ChatRoot">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBoolConverter x:Key="InverseBool"/>
        <converters:RoleToAlignmentConverter x:Key="RoleToAlign"/>
        <converters:RoleToBubbleBrushConverter x:Key="RoleToBubble"/>
        <converters:RoleToMaxWidthConverter x:Key="RoleToMaxWidth"/>
        <converters:EditDiffLineTypeToBrushConverter x:Key="DiffLineBrush"/>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer x:Name="ChatScrollViewer"
                      VerticalScrollBarVisibility="Auto"
                      Padding="12">
            <ItemsControl ItemsSource="{Binding Messages, ElementName=ChatRoot}" x:Name="MessagesControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:MessageViewModel}">
                        <StackPanel>
                            <!-- THINKING block — shows live thinking text while Claude reasons -->
                            <Border Margin="4,4,4,4"
                                    Padding="16,12"
                                    CornerRadius="8"
                                    MinWidth="250"
                                    MaxWidth="700"
                                    HorizontalAlignment="Left"
                                    Background="{StaticResource ThinkingBubbleBrush}"
                                    BorderBrush="{StaticResource PrimaryBrush}"
                                    BorderThickness="1"
                                    Visibility="{Binding IsThinking, Converter={StaticResource BoolToVis}}">
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                 From="1.0" To="0.6"
                                                                 Duration="0:0:0.8"
                                                                 AutoReverse="True"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>
                                <StackPanel>
                                    <!-- Header: indicator + label + timer + nudge -->
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#x25CF; " FontSize="18"
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="Claude is thinking..."
                                                   FontFamily="Nunito,Segoe UI,sans-serif"
                                                   FontSize="15"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding ThinkingDurationText}"
                                                   FontFamily="Cascadia Code,Consolas,monospace"
                                                   FontSize="13"
                                                   Foreground="{StaticResource MutedTextBrush}"
                                                   Margin="12,0,0,0"
                                                   VerticalAlignment="Center"/>
                                        <Button Content="Are You OK?"
                                                Margin="12,0,0,0"
                                                Padding="8,2"
                                                FontSize="11"
                                                Cursor="Hand"
                                                Command="{Binding CommandSource.NudgeCommand, ElementName=ChatRoot, FallbackValue={x:Null}}"
                                                ToolTip="Claude seems stuck. Click to send a nudge message."
                                                Visibility="{Binding CommandSource.ShowNudgeButton, ElementName=ChatRoot, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="4"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                                      VerticalAlignment="Center"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background"
                                                                                Value="{StaticResource SurfaceLightBrush}"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                    <!-- Streaming thinking text content -->
                                    <TextBox Text="{Binding ThinkingText, Mode=OneWay}"
                                             IsReadOnly="True"
                                             TextWrapping="Wrap"
                                             FontFamily="Cascadia Code,Consolas,Courier New"
                                             FontSize="12"
                                             FontStyle="Italic"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Padding="0,8,0,0"
                                             Foreground="{StaticResource MutedTextBrush}"
                                             SelectionBrush="{StaticResource SelectionBrush}"
                                             IsTabStop="False"
                                             Cursor="IBeam"
                                             MaxHeight="300"
                                             VerticalScrollBarVisibility="Auto"
                                             Visibility="{Binding HasThinkingText, Converter={StaticResource BoolToVis}}"/>
                                </StackPanel>
                            </Border>

                            <!-- Main message bubble -->
                            <Border Margin="4,4,4,4"
                                    Padding="12,8"
                                    CornerRadius="8"
                                    Visibility="{Binding IsThinking, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{Binding Role, Converter={StaticResource RoleToBubble}}"/>
                                        <Setter Property="HorizontalAlignment" Value="{Binding Role, Converter={StaticResource RoleToAlign}}"/>
                                        <Setter Property="MaxWidth" Value="{Binding Role, Converter={StaticResource RoleToMaxWidth}}"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsReviewerMessage}" Value="True">
                                                <Setter Property="Background" Value="{StaticResource ReviewerBubbleBrush}"/>
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="MaxWidth" Value="Infinity"/>
                                                <Setter Property="BorderBrush" Value="{StaticResource ReviewerAccentBrush}"/>
                                                <Setter Property="BorderThickness" Value="2,0,0,0"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <!-- Reviewer / participant badge -->
                                    <Border CornerRadius="3"
                                            Background="{StaticResource SurfaceLightBrush}"
                                            BorderBrush="{StaticResource ReviewerAccentBrush}"
                                            BorderThickness="1"
                                            Padding="6,2"
                                            Margin="0,0,0,4"
                                            HorizontalAlignment="Left"
                                            Visibility="{Binding IsReviewerMessage, Converter={StaticResource BoolToVis}}">
                                        <TextBlock Text="{Binding ReviewerLabel}"
                                                   FontSize="10" FontWeight="SemiBold"
                                                   Foreground="{StaticResource ReviewerAccentBrush}"/>
                                    </Border>
                                    <!-- Timestamp -->
                                    <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm'}"
                                               FontSize="10"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Opacity="0.5"
                                               Margin="0,0,0,2"/>
                                    <!-- Bookmark indicator -->
                                    <TextBlock Text="&#x2605;" FontSize="14"
                                               Foreground="{StaticResource AccentBrush}"
                                               HorizontalAlignment="Right"
                                               Margin="0,-2,0,0"
                                               Visibility="{Binding IsBookmarked, Converter={StaticResource BoolToVis}}"
                                               ToolTip="Bookmarked"/>
                                    <!-- Collapsible thinking content (shown after thinking phase completes) -->
                                    <Border Margin="0,0,0,6"
                                            CornerRadius="4"
                                            Background="{StaticResource ThinkingBubbleBrush}"
                                            BorderBrush="{StaticResource PrimaryBrush}"
                                            BorderThickness="1,0,0,0"
                                            Padding="8,6"
                                            Visibility="{Binding HasThinkingText, Converter={StaticResource BoolToVis}}">
                                        <StackPanel>
                                            <!-- Clickable header -->
                                            <Border Background="Transparent" Cursor="Hand"
                                                    MouseLeftButtonUp="ThinkingToggle_Click">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock FontSize="9" Margin="0,0,5,0"
                                                               Foreground="{StaticResource PrimaryBrush}"
                                                               VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Text" Value="&#x25B6;"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsThinkingExpanded}" Value="True">
                                                                        <Setter Property="Text" Value="&#x25BC;"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    <TextBlock Text="Thinking"
                                                               FontSize="12"
                                                               FontWeight="SemiBold"
                                                               Foreground="{StaticResource PrimaryBrush}"
                                                               VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                            <!-- Expandable thinking text -->
                                            <TextBox Text="{Binding ThinkingText, Mode=OneWay}"
                                                     IsReadOnly="True"
                                                     TextWrapping="Wrap"
                                                     FontFamily="Cascadia Code,Consolas,Courier New"
                                                     FontSize="12"
                                                     FontStyle="Italic"
                                                     Background="Transparent"
                                                     BorderThickness="0"
                                                     Padding="0,4,0,0"
                                                     Foreground="{StaticResource MutedTextBrush}"
                                                     SelectionBrush="{StaticResource SelectionBrush}"
                                                     IsTabStop="False"
                                                     Cursor="IBeam"
                                                     MaxHeight="400"
                                                     VerticalScrollBarVisibility="Auto"
                                                     Visibility="{Binding IsThinkingExpanded, Converter={StaticResource BoolToVis}}"/>
                                        </StackPanel>
                                    </Border>
                                    <!-- Main text (selectable) -->
                                    <TextBox Text="{Binding Text, Mode=OneWay}"
                                             IsReadOnly="True"
                                             TextWrapping="Wrap"
                                             FontFamily="Cascadia Code,Consolas,Courier New"
                                             FontSize="13.5"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Padding="0"
                                             Foreground="{StaticResource TextBrush}"
                                             SelectionBrush="{StaticResource SelectionBrush}"
                                             IsTabStop="False"
                                             Cursor="IBeam">
                                        <TextBox.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="Copy Selection" Command="ApplicationCommands.Copy"/>
                                                <MenuItem Header="Copy All" Click="CopyAllMessage_Click"
                                                          Tag="{Binding Text}"/>
                                                <Separator/>
                                                <MenuItem Header="Bookmark" Click="ToggleBookmark_Click"/>
                                            </ContextMenu>
                                        </TextBox.ContextMenu>
                                    </TextBox>

                                    <!-- Completion summary panel -->
                                    <Border Margin="0,8,0,0"
                                            CornerRadius="6"
                                            Background="{StaticResource SurfaceLightBrush}"
                                            BorderBrush="{StaticResource SuccessBrush}"
                                            BorderThickness="2,0,0,0"
                                            Padding="10,8"
                                            Visibility="{Binding HasCompletionSummary, Converter={StaticResource BoolToVis}}">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                <TextBlock Text="&#x2714;" FontSize="13"
                                                           Foreground="{StaticResource SuccessBrush}"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,6,0"/>
                                                <TextBlock Text="Summary"
                                                           FontSize="12"
                                                           FontWeight="SemiBold"
                                                           Foreground="{StaticResource SuccessBrush}"
                                                           VerticalAlignment="Center"/>
                                            </StackPanel>
                                            <TextBox Text="{Binding CompletionSummary, Mode=OneWay}"
                                                     IsReadOnly="True"
                                                     TextWrapping="Wrap"
                                                     FontFamily="Cascadia Code,Consolas,Courier New"
                                                     FontSize="13"
                                                     Background="Transparent"
                                                     BorderThickness="0"
                                                     Padding="0"
                                                     Foreground="{StaticResource TextBrush}"
                                                     SelectionBrush="{StaticResource SelectionBrush}"
                                                     IsTabStop="False"
                                                     Cursor="IBeam">
                                                <TextBox.ContextMenu>
                                                    <ContextMenu>
                                                        <MenuItem Header="Copy Selection" Command="ApplicationCommands.Copy"/>
                                                        <MenuItem Header="Copy Summary" Click="CopyAllMessage_Click"
                                                                  Tag="{Binding CompletionSummary}"/>
                                                    </ContextMenu>
                                                </TextBox.ContextMenu>
                                            </TextBox>
                                        </StackPanel>
                                    </Border>

                                    <!-- Attachments (screenshots, files sent with this message) -->
                                    <ItemsControl ItemsSource="{Binding Attachments}"
                                                  Visibility="{Binding HasAttachments, Converter={StaticResource BoolToVis}}"
                                                  Margin="0,6,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{StaticResource SurfaceLightBrush}"
                                                        CornerRadius="4" Padding="6,4" Margin="0,0,0,3"
                                                        Cursor="Hand"
                                                        MouseLeftButtonUp="AttachmentPreview_Click">
                                                    <StackPanel>
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="{Binding FileTypeIcon}" FontSize="12"
                                                                       Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                            <TextBlock Text="{Binding FileName}" FontSize="11"
                                                                       Foreground="{StaticResource PrimaryBrush}"
                                                                       VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                        <!-- Image thumbnail -->
                                                        <Image MaxHeight="200" MaxWidth="400"
                                                               Stretch="Uniform" StretchDirection="DownOnly"
                                                               Margin="0,4,0,0"
                                                               HorizontalAlignment="Left"
                                                               Tag="{Binding FilePath}"
                                                               Loaded="AttachmentImage_Loaded">
                                                            <Image.Style>
                                                                <Style TargetType="Image">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </Style>
                                                            </Image.Style>
                                                        </Image>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!-- Inline images (detected from file paths in text) -->
                                    <ItemsControl ItemsSource="{Binding InlineImages}"
                                                  Visibility="{Binding HasInlineImages, Converter={StaticResource BoolToVis}}"
                                                  Margin="0,6,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{StaticResource SurfaceLightBrush}"
                                                        CornerRadius="4" Padding="6,4" Margin="0,0,0,3"
                                                        Cursor="Hand"
                                                        MouseLeftButtonUp="InlineImage_Click">
                                                    <Image MaxHeight="300" MaxWidth="500"
                                                           Stretch="Uniform" StretchDirection="DownOnly"
                                                           HorizontalAlignment="Left"
                                                           Tag="{Binding}"
                                                           Loaded="AttachmentImage_Loaded">
                                                        <Image.Style>
                                                            <Style TargetType="Image">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </Style>
                                                        </Image.Style>
                                                    </Image>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!-- Tool uses — compact summary with expandable details -->
                                    <Border Background="{StaticResource ToolBubbleBrush}"
                                            CornerRadius="4" Padding="6,4" Margin="0,6,0,0"
                                            Visibility="{Binding HasToolUses, Converter={StaticResource BoolToVis}}">
                                        <Expander IsExpanded="True"
                                                  Foreground="{StaticResource TextSecondaryBrush}">
                                            <Expander.Header>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#x25CF; " FontSize="10"
                                                               Foreground="{StaticResource PrimaryBrush}"
                                                               VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding ToolActivitySummary}"
                                                               FontSize="12"
                                                               Foreground="{StaticResource PrimaryBrush}"
                                                               VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Expander.Header>
                                            <ItemsControl ItemsSource="{Binding ToolUses}" Margin="4,4,0,0">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate DataType="{x:Type vm:ToolUseViewModel}">
                                                        <Border Margin="0,1" Padding="4,2"
                                                                CornerRadius="3">
                                                            <StackPanel>
                                                                <StackPanel Orientation="Horizontal" Cursor="Hand">
                                                                    <TextBlock FontSize="9" Margin="0,0,5,0"
                                                                               Foreground="{StaticResource PrimaryBrush}"
                                                                               VerticalAlignment="Center">
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Setter Property="Text" Value="&#x25B6;"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                                                        <Setter Property="Text" Value="&#x25BC;"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                    <TextBlock Text="{Binding ToolName}"
                                                                               FontWeight="SemiBold"
                                                                               Foreground="{StaticResource PrimaryBrush}"
                                                                               FontSize="11.5"/>
                                                                    <TextBlock Text=": " FontSize="11.5"
                                                                               Foreground="{StaticResource TextSecondaryBrush}"
                                                                               Visibility="{Binding Summary, Converter={StaticResource BoolToVis}}"/>
                                                                    <TextBlock Text="{Binding Summary}"
                                                                               FontSize="11.5"
                                                                               Foreground="{StaticResource TextSecondaryBrush}"
                                                                               TextTrimming="CharacterEllipsis"
                                                                               MaxWidth="500"/>
                                                                    <StackPanel.InputBindings>
                                                                        <MouseBinding Gesture="LeftClick"
                                                                                      Command="{Binding ToggleExpandedCommand}"/>
                                                                    </StackPanel.InputBindings>
                                                                </StackPanel>
                                                                <!-- Expanded: show edit diff if available -->
                                                                <ItemsControl ItemsSource="{Binding EditDiffLines}"
                                                                              Margin="16,3,0,2"
                                                                              Visibility="{Binding HasEditDiff, Converter={StaticResource BoolToVis}}">
                                                                    <ItemsControl.Style>
                                                                        <Style TargetType="ItemsControl">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                            <Style.Triggers>
                                                                                <MultiDataTrigger>
                                                                                    <MultiDataTrigger.Conditions>
                                                                                        <Condition Binding="{Binding HasEditDiff}" Value="True"/>
                                                                                        <Condition Binding="{Binding IsExpanded}" Value="True"/>
                                                                                    </MultiDataTrigger.Conditions>
                                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                                </MultiDataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </ItemsControl.Style>
                                                                    <ItemsControl.ItemTemplate>
                                                                        <DataTemplate>
                                                                            <TextBlock Text="{Binding DisplayText}"
                                                                                       FontFamily="Cascadia Code,Consolas,Courier New"
                                                                                       FontSize="11"
                                                                                       Foreground="{Binding Type, Converter={StaticResource DiffLineBrush}}"
                                                                                       Padding="0,0.5"/>
                                                                        </DataTemplate>
                                                                    </ItemsControl.ItemTemplate>
                                                                </ItemsControl>
                                                                <!-- Expanded: show result preview (when no diff) -->
                                                                <TextBox Text="{Binding ResultPreview, Mode=OneWay}"
                                                                         IsReadOnly="True"
                                                                         TextWrapping="Wrap"
                                                                         FontFamily="Cascadia Code,Consolas,Courier New"
                                                                         FontSize="11"
                                                                         Background="Transparent"
                                                                         BorderThickness="0"
                                                                         Padding="0"
                                                                         Foreground="{StaticResource TextSecondaryBrush}"
                                                                         SelectionBrush="{StaticResource SelectionBrush}"
                                                                         IsTabStop="False"
                                                                         MaxHeight="200"
                                                                         VerticalScrollBarVisibility="Auto"
                                                                         Margin="16,3,0,2">
                                                                    <TextBox.Style>
                                                                        <Style TargetType="TextBox">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                            <Style.Triggers>
                                                                                <MultiDataTrigger>
                                                                                    <MultiDataTrigger.Conditions>
                                                                                        <Condition Binding="{Binding IsExpanded}" Value="True"/>
                                                                                        <Condition Binding="{Binding HasEditDiff}" Value="False"/>
                                                                                    </MultiDataTrigger.Conditions>
                                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                                </MultiDataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBox.Style>
                                                                </TextBox>
                                                            </StackPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Expander>
                                    </Border>

                                    <!-- Task/console output — collapsed by default -->
                                    <Border Background="{StaticResource ToolBubbleBrush}"
                                            CornerRadius="4" Padding="6,4" Margin="0,6,0,0"
                                            Visibility="{Binding HasTaskOutput, Converter={StaticResource BoolToVis}}">
                                        <StackPanel>
                                            <Expander IsExpanded="False"
                                                      Foreground="{StaticResource TextSecondaryBrush}">
                                                <Expander.Header>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="&#x25B6; " FontSize="10"
                                                                   Foreground="{StaticResource PrimaryBrush}"
                                                                   VerticalAlignment="Center"/>
                                                        <TextBlock Text="Console output"
                                                                   FontSize="12"
                                                                   Foreground="{StaticResource PrimaryBrush}"
                                                                   VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Expander.Header>
                                                <TextBox Text="{Binding TaskOutputText, Mode=OneWay}"
                                                         IsReadOnly="True"
                                                         TextWrapping="Wrap"
                                                         FontFamily="Cascadia Code,Consolas,Courier New"
                                                         FontSize="11"
                                                         Background="Transparent"
                                                         BorderThickness="0"
                                                         Padding="4"
                                                         Foreground="{StaticResource TextSecondaryBrush}"
                                                         SelectionBrush="{StaticResource SelectionBrush}"
                                                         IsTabStop="False"
                                                         MaxHeight="400"
                                                         VerticalScrollBarVisibility="Auto"
                                                         Margin="4,3,0,2"/>
                                            </Expander>
                                            <!-- Send to Claude / Sent indicator -->
                                            <Button Content="Send to Claude"
                                                    FontSize="11" Padding="8,2"
                                                    Margin="0,4,0,0"
                                                    HorizontalAlignment="Left"
                                                    Command="{Binding CommandSource.SendTaskOutputCommand, ElementName=ChatRoot, FallbackValue={x:Null}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource LinkButton}">
                                                <Button.Visibility>
                                                    <Binding Path="IsTaskOutputSent"
                                                             Converter="{StaticResource BoolToVis}"
                                                             ConverterParameter="Invert"/>
                                                </Button.Visibility>
                                            </Button>
                                            <TextBlock Text="Sent to Claude"
                                                       FontSize="11" Margin="0,4,0,0"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       Visibility="{Binding IsTaskOutputSent, Converter={StaticResource BoolToVis}}"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- AskUserQuestion buttons -->
                                    <ItemsControl ItemsSource="{Binding QuestionDisplay.Options}"
                                                  Visibility="{Binding HasQuestion, Converter={StaticResource BoolToVis}}"
                                                  Margin="0,8,0,0">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Margin="0,0,6,4"
                                                        Padding="12,6"
                                                        Cursor="Hand"
                                                        IsEnabled="{Binding DataContext.QuestionDisplay.IsAnswered,
                                                            RelativeSource={RelativeSource AncestorType=ItemsControl},
                                                            Converter={StaticResource InverseBool}}"
                                                        Command="{Binding CommandSource.AnswerQuestionCommand, ElementName=ChatRoot, FallbackValue={x:Null}}"
                                                        CommandParameter="{Binding Label}"
                                                        ToolTip="{Binding Description}">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                                                            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                            <Setter Property="BorderThickness" Value="1"/>
                                                            <Setter Property="FontSize" Value="12"/>
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border Background="{TemplateBinding Background}"
                                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                                CornerRadius="6"
                                                                                Padding="{TemplateBinding Padding}"
                                                                                Opacity="{TemplateBinding Opacity}">
                                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                                              VerticalAlignment="Center"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background"
                                                                                        Value="{StaticResource PrimaryBrush}"/>
                                                                                <Setter Property="Foreground"
                                                                                        Value="{StaticResource BackgroundBrush}"/>
                                                                            </Trigger>
                                                                            <Trigger Property="IsEnabled" Value="False">
                                                                                <Setter Property="Opacity" Value="0.4"/>
                                                                                <Setter Property="Cursor" Value="Arrow"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </Button.Style>
                                                    <TextBlock Text="{Binding Label}"/>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!-- Streaming indicator with timer and nudge button -->
                                    <StackPanel Orientation="Horizontal"
                                                Visibility="{Binding IsStreaming, Converter={StaticResource BoolToVis}}"
                                                Margin="0,4,0,0">
                                        <TextBlock Text="&#x25CF;" FontSize="16"
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   VerticalAlignment="Center">
                                            <TextBlock.Triggers>
                                                <EventTrigger RoutedEvent="Loaded">
                                                    <BeginStoryboard>
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="1" To="0.2"
                                                                             Duration="0:0:0.6"
                                                                             AutoReverse="True"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </TextBlock.Triggers>
                                        </TextBlock>
                                        <TextBlock Text="{Binding ThinkingDurationText}"
                                                   FontFamily="Cascadia Code,Consolas,monospace"
                                                   FontSize="13"
                                                   Foreground="{StaticResource MutedTextBrush}"
                                                   Margin="8,0,0,0"
                                                   VerticalAlignment="Center"/>
                                        <Button Content="Are You OK?"
                                                Margin="10,0,0,0"
                                                Padding="8,2"
                                                FontSize="11"
                                                Cursor="Hand"
                                                Command="{Binding CommandSource.NudgeCommand, ElementName=ChatRoot, FallbackValue={x:Null}}"
                                                ToolTip="Claude seems stuck. Click to send a nudge message."
                                                Visibility="{Binding CommandSource.ShowNudgeButton, ElementName=ChatRoot, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="4"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                                      VerticalAlignment="Center"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background"
                                                                                Value="{StaticResource SurfaceLightBrush}"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Scroll to bottom button -->
        <Button x:Name="ScrollToBottomButton"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,24,12"
                Panel.ZIndex="5"
                Visibility="Collapsed"
                Click="ScrollToBottomButton_Click"
                Cursor="Hand"
                ToolTip="Scroll to bottom">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="16"
                                        Padding="10,6">
                                    <TextBlock Text="&#x25BC; Down"
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource PrimaryBrush}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource MenuHighlightBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>

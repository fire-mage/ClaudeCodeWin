<Window x:Class="ClaudeCodeWin.WelcomeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:ClaudeCodeWin.Converters"
        Title="ClaudeCodeWin"
        Width="620" Height="650"
        MinWidth="450" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="28">

            <!-- Title -->
            <TextBlock Text="Welcome back!"
                       FontSize="22" FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}"
                       Margin="0,0,0,20"/>

            <!-- ===== Section 1: New Chat (recommended) ===== -->
            <Border x:Name="NewChatOption"
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="6" Padding="14,12" Margin="0,0,0,16"
                    Cursor="Hand"
                    BorderThickness="2"
                    BorderBrush="{StaticResource PrimaryBrush}"
                    MouseLeftButtonUp="NewChat_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="+" FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="New Chat" FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"/>
                            <Border Background="{StaticResource PrimaryDarkBrush}"
                                    CornerRadius="3" Padding="6,1" Margin="10,0,0,0"
                                    VerticalAlignment="Center">
                                <TextBlock Text="Recommended" FontSize="10" FontWeight="SemiBold"
                                           Foreground="#FFFFFF"/>
                            </Border>
                        </StackPanel>
                        <TextBlock x:Name="NewChatSubtitle"
                                   Text="Start a fresh conversation"
                                   FontSize="11"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>

                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>

            <!-- ===== Section 2: Switch Project ===== -->
            <Border x:Name="SwitchProjectSection"
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="6" Padding="14,12" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock x:Name="SwitchProjectHeader"
                               Text="Switch Project"
                               FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,10"/>

                    <!-- Project list -->
                    <ScrollViewer MaxHeight="250" VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ListBox x:Name="ProjectList"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 SelectionChanged="ProjectList_SelectionChanged"
                                 MouseDoubleClick="ProjectList_DoubleClick">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0,0,0,4"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="Bd" Background="{StaticResource BackgroundBrush}"
                                                        Padding="12,8" CornerRadius="6"
                                                        BorderThickness="2" BorderBrush="Transparent"
                                                        Cursor="Hand">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Bd" Property="BorderBrush"
                                                                Value="{StaticResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bd" Property="Background"
                                                                Value="{StaticResource SurfaceLightBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0"
                                                   Text="{Binding Name}"
                                                   FontSize="14" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextBrush}"/>

                                        <StackPanel Grid.Row="0" Grid.Column="1"
                                                    Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Background="{StaticResource PrimaryBrush}"
                                                    CornerRadius="3" Padding="5,1" Margin="8,0,0,0"
                                                    Visibility="{Binding IsCurrent, Converter={StaticResource BoolToVis}}">
                                                <TextBlock Text="Current" FontSize="10" FontWeight="SemiBold"
                                                           Foreground="{StaticResource BackgroundBrush}"/>
                                            </Border>
                                            <TextBlock Text="{Binding TechStack}"
                                                       FontSize="11"
                                                       Foreground="{StaticResource PrimaryBrush}"
                                                       VerticalAlignment="Center"
                                                       Margin="8,0,0,0"/>
                                        </StackPanel>

                                        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                                   Text="{Binding Path}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextTrimming="CharacterEllipsis"
                                                   Margin="0,2,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </ScrollViewer>

                    <!-- Action buttons -->
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button x:Name="StartWithProjectBtn"
                                Content="Start with selected project"
                                Style="{StaticResource PrimaryButton}"
                                Click="StartWithProject_Click"
                                Padding="16,6" Margin="0,0,8,0"
                                IsEnabled="False"/>
                        <Button Content="Browse..."
                                Click="BrowseProject_Click"
                                Padding="16,6"
                                Background="{StaticResource SurfaceLightBrush}"
                                Foreground="{StaticResource TextBrush}"
                                BorderThickness="0"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource BorderBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ===== Section 3: Continue Previous Chat ===== -->
            <Border x:Name="ContinueChatSection"
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="6" Padding="14,12" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Continue Previous Chat"
                               FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,10"/>

                    <ListView x:Name="RecentChatsList"
                              Background="Transparent"
                              BorderThickness="0"
                              Foreground="{StaticResource TextBrush}"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              SelectionChanged="RecentChats_SelectionChanged"
                              MouseDoubleClick="RecentChats_DoubleClick">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,4"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border x:Name="Bd" Background="{StaticResource BackgroundBrush}"
                                                    Padding="12,8" CornerRadius="6"
                                                    BorderThickness="2" BorderBrush="Transparent"
                                                    Cursor="Hand">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="BorderBrush"
                                                            Value="{StaticResource PrimaryBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background"
                                                            Value="{StaticResource SurfaceLightBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0"
                                               Text="{Binding Title}"
                                               FontSize="13" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextBrush}"
                                               TextTrimming="CharacterEllipsis"/>

                                    <TextBlock Grid.Row="0" Grid.Column="1"
                                               Text="{Binding UpdatedAt, StringFormat='{}{0:MMM dd, HH:mm}'}"
                                               FontSize="11"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"/>

                                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                                Orientation="Horizontal" Margin="0,3,0,0">
                                        <TextBlock Text="{Binding ProjectName}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource PrimaryBrush}"/>
                                        <TextBlock FontSize="11"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   Margin="10,0,0,0">
                                            <Run Text="{Binding MessageCount, Mode=OneWay}"/>
                                            <Run Text="messages"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Button x:Name="ContinueChatBtn"
                            Content="Continue"
                            Style="{StaticResource PrimaryButton}"
                            Click="ContinueChat_Click"
                            Padding="16,6" Margin="0,10,0,0"
                            IsEnabled="False"
                            HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>

            <!-- ===== Section 4: General Chat ===== -->
            <Border Background="Transparent" Cursor="Hand"
                    MouseLeftButtonUp="GeneralChat_Click"
                    Margin="0,0,0,8">
                <TextBlock FontSize="13" Foreground="{StaticResource TextSecondaryBrush}">
                    <Run Text="Or start a"/>
                    <Run Text="General Chat" Foreground="{StaticResource PrimaryBrush}"
                         TextDecorations="Underline"/>
                    <Run Text="without project binding"/>
                </TextBlock>
            </Border>

            <!-- Hint -->
            <TextBlock Text="Press Enter for new chat, Escape to close"
                       FontSize="11" FontStyle="Italic"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,4,0,0"/>

        </StackPanel>
    </ScrollViewer>
</Window>
